import { Container, Text } from "@/components";

export const SettingsPage = () => {
  return (
    <Container intent="flexColLeft" px="large">
      <Text intent="h3">Settings</Text>
    </Container>
  );
};
